const scrapeIt = require("scrape-it")

module.exports = () => 
{
    let pagesList,
        pagesTree
  
    let getPageData = (url, schema) =>{
        // Promise interface
        return scrapeIt(url, {
          
        })
    }
    
    let getPages = (mainUrl) => {
       
    }
    
    let getPageAssets = () => {
      
    }
    
    return {
       
    };
}
